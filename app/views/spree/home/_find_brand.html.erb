<% if !@texon.nil? %>
	<div class="field">
	  <%= select_tag 'taxon_id', options_for_select(@texon.collect{ |c| [c.name, c.id] }),  :class => 'dropdown_menu',:prompt => "Select Brand" %>
	</div>
<% end %>	
